# Trackable diagnostic reports

Small, sanitized run summaries exported from `outputs/` belong here and may be
committed to GitHub for remote diagnosis. Never add datasets, model checkpoints,
raw images, full videos, credentials, or large per-frame tables.

