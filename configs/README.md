# Configuration policy

Configurations are grouped into `datasets/`, `models/`, `experiments/`, and
`ablations/`. Phase 0 intentionally defines no executable defaults: dataset
paths, model revisions, thresholds, seeds, and protocols will be added only
with their Phase 1 implementations and validation.

