# Analysis artifacts

This directory stores immutable, versioned dataset audits and natural failure analyses.
It is separate from model run outputs. Each audit records its seed, source split, missing
attributes, and whether the source frames form a valid continuous sequence.

